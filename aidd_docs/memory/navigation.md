# Navigation

Les conventions de navigation du frontend.

- Routes déclarées dans `frontend/src/app/app.route.ts`, vue principale : le kanban des tâches.
- Routeur Angular activé via `app.config.ts` (application standalone).
